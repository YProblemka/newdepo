$(document).ready(function () {
    $(".jb-slider").slick({
        autoplay: true,
        infinite: false,
        autoplaySpeed: 2000,
        speed: 1000,
        slidesToShow: 1,
        prevArrow: false,
        nextArrow: false,
        // vertical: true,
        // dots: true,
        // prevArrow: $(".slider__arrows .btn-prev"),
        // nextArrow: $(".slider__arrows .btn-next"),
    });
});
